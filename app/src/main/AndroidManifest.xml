<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ouicoding">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".Service.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launch_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launch_new"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".View.MainActivity"/>
        <activity android:name=".View.DashboardEmploye" />
        <activity android:name=".View.DashboardRh" />
        <activity android:name=".View.Register" />
        <activity android:name=".Service.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>